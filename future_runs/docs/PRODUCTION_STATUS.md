# 七个正式 run 的执行记录

最后更新：2026-09-09 16:05

预检与配置见 [`BATCH1_PRECHECK.md`](BATCH1_PRECHECK.md)，本文件只记录**提交之后**
发生的事。七个 case 的分段与并行设置完全一致：`STOP_N=11`、`REST_N=11`、
`RESUBMIT=6`、`NTASKS_LND=2560`、20 节点、4 h 墙钟，共用 T4 构建
（md5 `e8b487b04a27adfd8f683359dfd73df5`）。

## 总览

| 情景 | 批 | 首段作业 | 状态 |
|---|---|---|---|
| ssp119 | 1 | 518000 | **完成**，2024–2101 |
| ssp370 | 1 | 518002 | **完成**，2024–2101 |
| ssp245 | 1 | 518001 | 末段运行中（520147） |
| ssp585 | 2 | 522045 | 首段运行中 |
| ssp370_RF | 2 | 522046 | 首段运行中 |
| ssp370_DF | 2 | 522047 | PENDING |
| ssp370_RH | 3 | 522138 | PENDING |

批 1 于 2026-09-08 19:22 提交，批 2 于 2026-09-09 15:35 提交，
批 3 于 2026-09-09 16:00 提交。三批的提交前检查项相同，见预检文档。

## 批 1 的完成情况

**ssp119 与 ssp370 已跑满 77 年**，两者均为 77 个 `elm.h0` 文件、7 套 restart，
末次存档 `2101-01-01`，RUNDIR 各 1.6 TiB。ssp245 在第七段（520147），
其余指标与前两者一致。

七段的 restart 年份：2035 / 2046 / 2057 / 2068 / 2079 / 2090 / 2101。

**全部 21 个已完成段退出码均为 `0:0`。** 对每个 case 的全部分段日志做
`zgrep`（日志成功后被 CIME 压缩，必须用 `zgrep` 而非 `grep`），
`ENDRUN`、`SIGSEGV`、`Segmentation`、balance error 的命中数均为 **0**。

### 单段实测用时

| 情景 | 段数 | 最短 | 最长 | 积分合计 |
|---|---|---|---|---|
| ssp119 | 7 | 1:50:55 | 2:25:28 | 约 15.7 h |
| ssp370 | 7 | 1:51:38 | 2:22:07 | 约 14.2 h |

4 h 墙钟对 11 年一段留了近一倍余量。含段间排队的实际墙上时间，
ssp119 约 19.4 h、ssp370 约 16.9 h。**自动续投全程无需干预。**

## 集群负载（2026-09-09 15:42 观测）

| 分区 | 已用核 | 空闲核 | 总核 |
|---|---|---|---|
| parallel | 20915 | 3653 | 24696 |
| hpcl-cli185（专用） | 1848 | 712 | 2560 |

`parallel` 的 140 个 `blc` 节点中 **没有一个完全空闲**（116 allocated、
17 mixed、6 mixed-、1 drained），空闲核零散分布在混用节点上，凑不出 20 个整节点。
专用分区被同项目另一用户的两个 10 节点作业占去一半。
`parallel` 上有 1389 个 pending 作业，合计请求 4130 节点。

即便如此，批 2/3 的等待并不长：ssp585 提交后 7 分钟起跑，RF 提交后 29 分钟起跑，
均早于调度器按满墙钟预留给出的保守预测。`--constraint=BL` 加
`--exclude=blc051,blc052` 是主要的等待来源，但不应为缩短排队而放宽，
多节点 MPI 需要 BL 节点的网络拓扑。

## 存储

2026-09-09 删除 `/scratch/hpcl-cli185/zw5/e3sm_run_moved_from_proj_shared_20260831`，
16 个 2026 年 2/4/6 月的旧 case run 目录，5.1 TiB、36285 文件，用时 36 秒。

删除前逐项确认：`e3sm_cases` 下所有 case 的 `user_nl_*` 与 `env_*.xml` 均不引用该路径；
无任何 `RUNDIR`/`EXEROOT` 指向该目录；当前生产血统
（20260712 ad_spinup → 20260717 spinup → 20260723 harvfix → 20260902 smoothHARV）
完整保留在 proj-shared 的 `e3sm_run/`。

> **该目录是唯一副本。** 2026-08-31 那次是 `rsync` **移动**而非复制，
> proj-shared 的原件已不存在。此类删除不可逆，执行前必须确认这一点。

| 时刻 | 项目 `/scratch` 用量 |
|---|---|
| 删除前 | 194.3 TiB |
| 删除后 | 191.7 TiB |

净回收 2.6 TiB 而非 5.1 TiB，因为批 1 与 ssp585 在同一时段仍在写输出，
且 Lustre 的对象释放是异步的。软限 200 TiB、硬限 400 TiB。
七个 run 跑满预计落在 198 TiB 上下，在软限内。

## 一次值得记录的核对：RF 的 landuse 日期

RF 的 `flanduse_timeseries` 文件时间戳为 **2026-08-19**，早于 8 月 28 日的
harvest smoothing 修复，表面上像是漏更新。核对结论是**正确的**：该次修复只影响
Default 与 RH 两组，RF、DF 与历史组不受影响。DF 与 RH 的文件均为 2026-09-05。

| case | landuse 文件日期 |
|---|---|
| ssp119 / ssp245 / ssp370 / ssp585 | 2026-09-05 |
| ssp370_DF / ssp370_RH | 2026-09-05 |
| ssp370_RF | 2026-08-19（情景无关，不受修复影响） |

## 未决事项

**`SEEDC_GRC` 在每个段边界被重置。** 测试 2a 已确认这是一个不随 restart 保存的
格点级诊断量，在分段起点归零；restart 状态本身与续跑前完全一致
（0.5° 上 462 个 restart 变量全等）。七个 run 各有 6 个段边界，
因此七套 h0 输出都会带上同样的痕迹。

处置仍未定：接受并记录 / 从 h0 输出中去掉该变量 / 查上游 E3SM 是否为已知缺口。
**用到 h0 里的 `SEEDC_GRC` 做分析之前必须先定。不因为量级小就当作通过。**

另外，README「待验证」一节中的 4 km future 末端行为（2100-12-29 → 2101-01-01
的实际驱动、守卫是否触发、是否存在 `yr=2101` 的 import）现已有 ssp119 与 ssp370
两个跑到 2101-01-01 的完整 run 可供检查，尚未做。
